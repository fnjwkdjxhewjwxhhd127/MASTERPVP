name: MASTERPVP CONTROL
on:
  push:
    paths:
      - 'links.txt'

permissions:
  contents: write

jobs:
  switch_vpn:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Process VPN Files
        run: |
          python - <<EOF
          import os
          import base64

          links_file = "links.txt"

          target_files = [
              "vpn.txtMASTER",
              "vpn.txtMASTER1",
              "vpn.txtMASTER2",
              "vpn.txtMASTER3",
              "vpn.txtMASTER4",
              "vpn.txtMASTER5",
              "vpn.txtMASTER6",
              "vpn.txtMASTER7",
              "vpn.txtMASTER8",
              "vpn.txtMASTER9"
          ]

          if not os.path.exists(links_file):
              print(f"Ошибка: Файл {links_file} не найден!")
              exit(1)
              
          with open(links_file, "r", encoding="utf-8") as f:
              content = f.read().strip()
              
          if not content or content.lower() == "off":
              print("В links.txt пусто или написано OFF. Ломаем файлы...")
              off_text = "OFF_vpn".encode("utf-8")
              off_base64 = base64.b64encode(off_text).decode("utf-8")
              
              for file_name in target_files:
                  with open(file_name, "w", encoding="utf-8") as f:
                      f.write(off_base64)
                  print(f"Файл {file_name} отключен.")
          else:
              print("Шифруем новые ссылки из links.txt в Base64...")
              encoded_bytes = base64.b64encode(content.encode("utf-8"))
              base64_string = encoded_bytes.decode("utf-8")
              
              for file_name in target_files:
                  with open(file_name, "w", encoding="utf-8") as f:
                      f.write(base64_string)
                  print(f"Файл {file_name} успешно обновлен и зашифрован.")
          EOF

      - name: Save changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # Настоящая магия здесь: точка означает добавить ВСЕ измененные файлы в репозитории
          git add .
          
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto-update VPN: All 10 files updated via links.txt"
            git push
          fi
          
          
